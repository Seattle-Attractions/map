$("td:contains('Show')").show();
$('#<%= dom_id(@restaurant) %>').find("td:contains('Show')").hide();
$('#sidebar').empty().append(
  "<%= j render(
  partial: 'layouts/global_show',
  locals: { fields: @fields, record: @restaurant }) %>"
);
