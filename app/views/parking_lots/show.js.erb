$("td:contains('Show')").show();
$('#<%= dom_id(@parking_lot) %>').find("td:contains('Show')").hide();
$('#sidebar').empty().append(
  "<%= j render(
  partial: 'layouts/global_show',
  locals: { fields: @fields, record: @parking_lot }) %>"
);
